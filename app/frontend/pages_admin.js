// F7 — admin panel + monitoring. (Placeholder until Stage F7.)
import { el, mount } from '/static/lib.js';

export function renderAdmin(view) { mount(view, el('p.muted', {}, 'Admin panel — coming in F7.')); }
